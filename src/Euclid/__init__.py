"""Euclid-DR3-like survey configuration for the shared GLASS forward model.

Mirrors `src/KiDS/` but for a 14 000 deg^2 equatorial band with 13 tomographic bins and no
survey systematics. Nothing here is protected physics: the forward model itself is reused
unchanged from `src/cosmology/`.
"""
