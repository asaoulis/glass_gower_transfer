# all the usual ignores
*.pyc
*.npz
*.npy
*.pkl
*.sh
*.log
*.out
iolaus/
__pycache__/
.vscode/
.DS_Store
.ipynb_checkpoints
nonLimber_matter_shells/
kids-legacy-sbi/
glass/
*.h5
build/
*.so.0
*.err
*.ipynb
# ...except this one: a self-contained, output-free comparison notebook meant to be
# shared with collaborators alongside the posterior bundle (see scripts/).
!scripts/euclid_posterior_comparison.ipynb
wandb/
*.json
# Committed data files: locked fixed test-set sim_id lists (override the *.json ignore)
!config/fixed_test_sets/
!config/fixed_test_sets/*.json
lightning_logs/
sbi-logs/
logs/
*.png
*.svg
*.tch
*.ckpt
.claude/
CLAUDE.md
# validation shell-projection matter_cls disk cache
.glass_matter_cache/
*.pt
# local analysis working state (shear-replay sweeps, fetched checkpoints, rank dumps)
ml-checkpoints
steps_*.jsonl
rank_table.txt